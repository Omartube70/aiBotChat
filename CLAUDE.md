# بوت القدس لمهمات المصاعد + توب باور (واتساب)

الملف ده بيتقري أوتوماتيك في أي جلسة Claude (من الكمبيوتر أو التليفون). **حدّثه لما تتفق مع صاحب المحل على قاعدة جديدة.**
صاحب المحل (حاج محمد) بيكتب بالعامية المصرية ومش مبرمج — رد عليه بالعربي البسيط، ونفّذ وانشر من غير ما تطلب منه ينسخ حاجة.

## النشر (مهم)
- أي commit على `main` بيتنشر لوحده على Cloudflare عن طريق `.github/workflows/deploy.yml` (محتاج secret `CLOUDFLARE_API_TOKEN` في GitHub).
- البوت الحي: `https://quds-bot.omartubefaheen.workers.dev` (حساب Cloudflare بتاع omartubefaheen).
- سجل البوت من الموبايل: `/debug/log` (رسايل وصلت + مشاكل إرسال + ردود اتمنعت عشان أسعار متألفة).
- فيه بوت **قديم** على `quds-bot.toppowerelevators4.workers.dev` (حساب تاني). رقم القدس متحوّل للجديد بـ override على مستوى الرقم
  (phone number id `1343277232194983`, `webhook_configuration.override_callback_uri`). **متنشرش على الحساب القديم ولا من فولدر "New folder" على سطح المكتب.**
- لو "حاجة مش شغالة" — اتأكد الأول إن الرسايل بتوصل للبوت ده (`npx wrangler tail` أو `/debug/log`) قبل ما تعدّل كود.

## النشاطين
- **القدس**: بيع قطع غيار مصاعد بس (الأسعار من إنياد: `src/catalog.js`).
- **توب باور**: تركيب مصاعد كاملة (عرض سعر Word بـ 6 أسئلة: الاسم، العنوان، التليفون، نوع الماكينة، قدرة الماكينة، عدد الأدوار) + صيانة (الهضبة وحدائق الأهرام بس).
- التفرقة بالكلمات: `wantsElevatorQuote` / `wantsMaintenance` في `src/worker.js`. لو مش واضح → 3 زراير (تركيب / قطع غيار / صيانة).

## أرقام الموظفين (wrangler.toml)
- التلاتة: `201000363323` (حاج محمد — المدير)، `201003044660`، `201050699420`.
- التلاتة بياخدوا سعر البيع والشراء (`AGENT_ADMINS`) ونسخة من رسايل الزباين (`CC_NUMBERS`).
- **الرواتب من `201000363323` بس** (`AGENT_PAYROLL`). المعادلة: قيمة القبض ÷ **12** = قيمة اليوم، × أيام الحضور − السلف (`src/payroll.js`). **مش ÷ 24.**
- تذكير أوتوماتيك قبل ما نافذة واتساب (24 ساعة) تقفل لأرقام الموظفين (`remindStaffWindow`، cron كل 30 دقيقة).
- الموظفين بالأسئلة: "موظف جديد" (اسم ← راتب)، "حذف موظف" (قايمة بأرقام)، "الموظفين".
- **أرقام الموظفين التلاتة = البوت مساعدهم، مش بيّاع.** أي كلام (كتابة أو فويس، بـ"يا بوت" أو من غيرها) بيروح لـ `handleStaffRequest`:
  سعر بيع/شراء منتج، فاتورة (عدد × سعر + إجمالي)، عرض تركيب، رواتب وموظفين، "قول لعمر كذا"، "هاني مبروك بقاله كتير ابعتله رسالة حلوة"،
  "حط سعر عرض الأستاذ فلان 450 ألف". لو الاسم يطابق زبون واحد بيبعت على طول، لو أكتر بيسأل "تقصد مين؟" (رقم / التاني / اللي آخره ...).
- دفتر الزباين الدائم `customers:dir` (مش بس آخر 100) + أسماء جوجل لو اتربطت.
- حسابات العملاء والموردين (مديونيات) **مش متاحة** — محتاجة ملف Excel من إنياد.
- واتساب مابيسمحش نبعت رسالة عادية لزبون عدّى 24 ساعة على آخر رسالة منه — محتاج Template معتمد من ميتا (بفلوس).
  صاحب المحل **مش عايز يدفع** على رسايل التسويق: الزبون اللي عدّى 24 ساعة البوت بيدّي الموظف لينك wa.me فيه الرسالة جاهزة
  يبعتها من تليفونه ببلاش (`sendStaffMessage`). "كلّم هاني وإسلام وعمر" بتتنفّذ لكل واحد. متعملش إرسال جماعي بفلوس من غير ما يطلب.

## قواعد الرد على الزباين
- **ممنوع أي منتج أو سعر مش من الكتالوج.** فيه قفل في الكود (`pricesGrounded` في `src/gemini.js`) — متشيلوش.
- الترحيب في أول رسالة: "أهلاً يا أستاذ <الاسم>" (أو اللقب المتسجل زي "حاج سيد")، ولو مفيش اسم "أهلاً بحضرتك". **ممنوع "يا حاج محمد" لأي زبون** — دي للمدير بس.
- البحث بيستحمل "ال" زيادة/ناقصة، الجمع (وايرات/كوالين)، والأخطاء (صالون→كالون) ويقول "تقصد كذا؟".
- الأسلوب: بيّاع مصري ودود بيندمج مع الزبون، رسايل قصيرة، الأسعار في نقاط، وكل الصور (لحد 10).
- الفويس: الرد مكتوب **الأول دايمًا** وبعده الصور والنسخ، والصوت (Gemini TTS → MP3 بـ lamejs) **آخر حاجة** — لأن الـ MP3 تقيل وCloudflare المجاني (10ms معالجة، 50 نداء) ممكن يقطع الرسالة. `VOICE_REPLIES` في wrangler.toml.
- الكتالوج محفوظ في KV (`catalog:snapshot`، بيتحدث كل 30 دقيقة) والبحث محلي — ممنوع ترجّع نداءات إنياد لكل رسالة. صور المنتجات بتعدّي على images.weserv.nl (WebP → JPEG).

## ملاحظات تقنية
- Gemini: `gemini-flash-lite-latest` بـ `thinkingLevel: minimal` (سرعة)، واحتياطي بالتوازي لو اتأخر (`raceGemini`).
- صور إنياد امتدادها `.png` بس فعليًا WebP/JPEG — بتترفع لواتساب بنوعها الحقيقي (`sendImage`).
- الحالة في D1 (`src/d1kv.js`) والكاش في KV `MEMORY`. محادثة الزبون بتتنسي بعد 30 دقيقة سكوت.
- أسماء جهات اتصال جوجل: محتاجة `GOOGLE_CLIENT_ID` و`GOOGLE_CLIENT_SECRET` (لسه مش متحطوطين) وبعدها `/google/connect`.
- اختبار سريع من غير واتساب: `/debug/chat?text=...` و `/debug/search?q=...`.
